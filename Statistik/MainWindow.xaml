<Window x:Class="Statistik.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Statistik"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="850
        " SizeChanged="Window_SizeChanged"
        MinHeight="600" MinWidth="850">
    <Grid DataContext="{StaticResource mvmodel}">
        <DataGrid x:Name="dataGrid" Margin="10,10,90,0" AutoGenerateColumns="False" ItemsSource="{Binding DataSets}" Height="102" VerticalAlignment="Top" CellEditEnding="dataGrid_CellEditEnding">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Name}" Header="Name"/>
                <DataGridTextColumn Binding="{Binding Value}" Header="Wert"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="btnAdd" Content="Neu" HorizontalAlignment="Right" Margin="705,10,10,10" VerticalAlignment="Top" Width="70" Panel.ZIndex="2"/>
        <Button x:Name="BtnRem" Content="Entfernen" IsEnabled="False" HorizontalAlignment="Right" Margin="705,35,10,0" VerticalAlignment="Top" Width="70" Panel.ZIndex="2"/>
        <Canvas x:Name="cnvBalken" Margin="10,117,10,10" Background="#FFFFF0F0"/>

    </Grid>
</Window>
